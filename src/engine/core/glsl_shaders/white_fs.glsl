 // também conhecido como "pixelshader"
// executada para cada pixel a ser desenhado na tela
void main(void){
    // gl_FragColor determina a cor do pixel
    // neste caso, branco.
    gl_FragColor = vec4(1,0.7,1,1.0);
}